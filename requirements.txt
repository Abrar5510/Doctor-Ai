# Python Version Requirement: 3.11.x
# See .python-version and runtime.txt for version pinning

# Build Tools - setuptools >= 65.5.0 doesn't require distutils (needed for Python 3.12+)
setuptools>=70.0.0

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# Vector Database & ML Libraries
# REQUIRED: Qdrant is now the primary database (replacing PostgreSQL)
# ML libraries needed for generating embeddings for semantic search
qdrant-client==1.7.3
transformers==4.35.2
torch==2.1.1
numpy==1.26.2

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Caching
redis==5.0.1

# Utilities
python-dotenv==1.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.25.2

# Logging & Monitoring
loguru==0.7.2

# Data Processing - pandas removed (~30MB)
# Only used for reading CSV demo data - can use stdlib csv module instead

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0

# AI Assistant (LLM)
openai==1.3.7

# PDF Export
reportlab==4.0.7
