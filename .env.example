# Application Settings
APP_NAME="Medical Symptom Constellation Mapper"
APP_VERSION="0.2.0"
DEBUG=True
LOG_LEVEL=INFO

# API Settings
API_HOST=0.0.0.0
API_PORT=8000
API_PREFIX=/api/v1

# Security
# REQUIRED: Generate a strong secret key for production:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# MUST be at least 32 characters long
SECRET_KEY=CHANGEME_GENERATE_SECURE_SECRET_KEY_AT_LEAST_32_CHARS_LONG
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings
# Comma-separated list of allowed origins (wildcards not allowed for security)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost:8080
CORS_ALLOW_CREDENTIALS=True

# Rate Limiting
RATE_LIMIT_ENABLED=True
RATE_LIMIT_REQUESTS=100  # requests per minute
RATE_LIMIT_PERIOD=60     # seconds

# ============================================================
# PRIMARY DATABASE: Qdrant Vector Database
# ============================================================
# Production: Qdrant Cloud (https://cloud.qdrant.io/) - FREE tier available
# Local Dev: Docker Compose with 'localhost'
# Data Source: CSV files in /data/ directory (106 records)

# Qdrant Cloud Configuration (Recommended for Production)
# 1. Sign up at https://cloud.qdrant.io/ (FREE 1GB cluster)
# 2. Create cluster → Copy URL (e.g., xxxxx.us-east-1.aws.cloud.qdrant.io)
# 3. Generate API key → Settings → API Keys
QDRANT_HOST=your-cluster.us-east-1.aws.cloud.qdrant.io
QDRANT_PORT=6333
QDRANT_API_KEY=your_qdrant_api_key_here
QDRANT_COLLECTION_NAME=medical_conditions

# Local Development with Docker (Optional)
# Uncomment for local testing before uploading to cloud:
# QDRANT_HOST=localhost
# QDRANT_PORT=6333
# QDRANT_API_KEY=  # Leave empty for local Docker

# Redis Cache (Optional - set REDIS_ENABLED=True to enable)
REDIS_ENABLED=False
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ML Models
EMBEDDING_MODEL=microsoft/BiomedNLP-PubMedBERT-base-uncased-abstract-fulltext
EMBEDDING_DIMENSION=768
MAX_SEQUENCE_LENGTH=512

# AI Assistant Configuration
# Option 1: Use OpenAI directly
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-CHANGEME_YOUR_OPENAI_API_KEY_HERE

# Option 2: Use OpenRouter (supports multiple AI providers including OpenAI)
# Get your API key from: https://openrouter.ai/keys
USE_OPENROUTER=False
OPENROUTER_API_KEY=sk-or-v1-CHANGEME_YOUR_OPENROUTER_API_KEY_HERE
# Available models: openai/gpt-4-turbo-preview, openai/gpt-3.5-turbo, anthropic/claude-3-opus, etc.
OPENROUTER_MODEL=openai/gpt-4-turbo-preview

# Option 3: Use local LLM (Ollama)
USE_LOCAL_LLM=False
LOCAL_LLM_MODEL=llama2

# Medical Ontology
USE_SNOMED_CT=True
USE_ICD10=True
USE_HPO=True

# Confidence Thresholds
TIER1_CONFIDENCE_THRESHOLD=0.85
TIER2_CONFIDENCE_THRESHOLD=0.60
TIER3_CONFIDENCE_THRESHOLD=0.40

# Search Parameters
TOP_K_CANDIDATES=50
FINAL_RESULTS_LIMIT=10

# HIPAA Compliance
ENABLE_AUDIT_LOGGING=True
ENABLE_DATA_ANONYMIZATION=True
AUDIT_LOG_PATH=./logs/audit/

# Feature Flags
ENABLE_RARE_DISEASE_DETECTION=True
ENABLE_RED_FLAG_ALERTS=True
ENABLE_TEMPORAL_ANALYSIS=True
ENABLE_AI_ASSISTANT=True
